services:
  - type: web
    name: love-quiz-privacy-demo
    env: php
    buildCommand: exit 0   # no build needed
    startCommand: php -S 0.0.0.0:$PORT
    plan: free
    autoDeploy: true
    rootDir: .
    envVars:
      PHP_VERSION: 8.2
